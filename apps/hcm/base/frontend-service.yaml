apiVersion: v1
kind: Service
metadata:
  name: hcm-frontend
  labels:
    app: hcm-frontend
spec:
  type: ClusterIP
  selector:
    app: hcm-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
